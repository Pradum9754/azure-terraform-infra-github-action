name: "azure-terraform-infra"

on: 
  push: 
    branches: 
        - 'feature/**'
        - 'main'
        - 'hotfix/**'
        
jobs:
- job:
  terraforminit:
    name: terraform init
    runs-on: github-actions
    steps: 
    - name: "azure-terraform-infra"
      run: echo azure-terraform-infra

job:
  terraformplan:
    name: terraform plan
    runs-on: github-actions
    steps: 
    - name: "azure-terraform-infra"
      run: echo azure-terraform-infra
